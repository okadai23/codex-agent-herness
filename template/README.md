# {{ project_name }}

Generated by product-standard-template.


## Agent runtime

このテンプレートは `agent_runtime`（`codex` / `claude` / `dual`）を受け取り、運用時の想定ランタイムを表現します。

- `codex`: Codex中心運用
- `claude`: Claude Code中心運用
- `dual`: 両ランタイム併用

Claude Code向けの最小指針は `./.claude.instructions.md` を参照してください。


## 生成ファイル

- `codex`: `.codex/**` を生成し、`.claude.instructions.md` は生成しない。
- `claude`: `.claude.instructions.md` を生成し、`.codex/**` は生成しない。
- `dual`: `.codex/**` と `.claude.instructions.md` の両方を生成する。
